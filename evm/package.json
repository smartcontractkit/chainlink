{
  "name": "chainlink",
  "version": "0.6.5",
  "license": "MIT",
  "scripts": {
    "build": "truffle build",
    "test": "truffle test",
    "lint": "echo \"No linter configured for evm project\" &&  exit 0",
    "slither": "truffle build && (slither . || (echo 'slither not installed' && exit 1))"
  },
  "devDependencies": {
    "truffle": "^5.0.25"
  },
  "files": [
    "contracts",
    "v0.5/contracts",
    "zos.json",
    "zos.rinkeby.json",
    "zos.ropsten.json"
  ]
}
