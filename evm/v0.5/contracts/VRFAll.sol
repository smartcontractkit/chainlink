pragma solidity 0.5.0;

import "./dev/VRF.sol";
import "./tests/VRFTestHelper.sol";

contract VRFAll {
  // Dummy, unused contract.
  //
  // This exists because sol-compiler gets confused unless there is a contract
  // of the same name in the target solidity file. ("Cannot read property
  // 'VRFAll' of undefined".)
}
