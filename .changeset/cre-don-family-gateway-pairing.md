---
"chainlink": patch
---

Scope local CRE gateway connectors and workers by explicit nodeset `don_family` for multi-family topologies. Capabilities registry and workflow deploy use TOML `don_family` (legacy topologies fall back to `config.DefaultDONFamily`). #internal
