---
"chainlink": patch
---

#updated Add `fromAddressIndex` config parameter to avoid having to specify a specific ETH address. This is a backward compatible change because `fromAddress` takes precedence if it exists.
