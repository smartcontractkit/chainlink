// SPDX-License-Identifier: MIT
// Warning: this is an autogenerated file! DO NOT EDIT.

pragma solidity ^0.8.6;

contract StakingPoolLibEventsMock {
    event FeedOperatorsSet(address[] feedOperators);
    event MaxCommunityStakeAmountIncreased(uint256 maxStakeAmount);
    event MaxOperatorStakeAmountIncreased(uint256 maxStakeAmount);
    event OperatorAdded(address operator);
    event OperatorRemoved(address operator,uint256 amount);
    event PoolConcluded();
    event PoolOpened();
    event PoolSizeIncreased(uint256 maxPoolSize);
    function emitFeedOperatorsSet(address[] memory feedOperators) public {
        emit FeedOperatorsSet(feedOperators);
    }
    function emitMaxCommunityStakeAmountIncreased(uint256 maxStakeAmount) public {
        emit MaxCommunityStakeAmountIncreased(maxStakeAmount);
    }
    function emitMaxOperatorStakeAmountIncreased(uint256 maxStakeAmount) public {
        emit MaxOperatorStakeAmountIncreased(maxStakeAmount);
    }
    function emitOperatorAdded(address operator) public {
        emit OperatorAdded(operator);
    }
    function emitOperatorRemoved(address operator,uint256 amount) public {
        emit OperatorRemoved(operator,amount);
    }
    function emitPoolConcluded() public {
        emit PoolConcluded();
    }
    function emitPoolOpened() public {
        emit PoolOpened();
    }
    function emitPoolSizeIncreased(uint256 maxPoolSize) public {
        emit PoolSizeIncreased(maxPoolSize);
    }
}
