WorkflowRegistryManager.getVersion
├── when versionNumber is not registered
│   └── it should revert with VersionNotRegistered
└── when versionNumber is registered
    └── it should return the correct version details
