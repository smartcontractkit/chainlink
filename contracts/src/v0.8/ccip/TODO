- [ ] Update CI/CD workflows to build ccip-contracts and release
- [x] Confirm .changeset/config.json ignore: [] will ignore correct CCIP path
- [x] Ignore .changeset/ and other directories/files (README/CHANGELOG) when building ccip-contracts
- [x] Update ccip README and CHANGELOG
- [ ] Set version and other fields of ./ccip/package.json - confirm abi path for files?
- [x] Let's make sure we add CCIP to the ignore before we release the next chainlink package so it will never be part of it
- [ ] Update package.json build scripts
- [ ] Figure out ABI path?
- [ ] Figure out what deps we can remove from ccip/package.json
- [ ] Confirm if "compile:native": "./../../../scripts/native_solc_compile_all" will run correctly with the right paths (relative vs absolute etc)?
