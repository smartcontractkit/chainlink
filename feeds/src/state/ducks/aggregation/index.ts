import reducer from './reducers'

import * as aggregationOperations from './operations'
import * as aggregationSelectors from './selectors'

export { aggregationOperations, aggregationSelectors }

export default reducer
