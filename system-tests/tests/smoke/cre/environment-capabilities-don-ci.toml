
[blockchain_a]
  type = "anvil"
  docker_cmd_params = ["-b", "5"]

[jd]
  image = "injected-at-runtime"

[infra]
  # either "docker" or "crib"
  type = "docker"

[workflow_config]
  workflow_name = "abcdefgasd"
  feed_id = "018bfe8840700040000000000000000000000000000000000000000000000000"

  use_cre_cli = true
  should_compile_new_workflow = false

  [workflow_config.dependencies]
  cron_capability_binary_path = "./cron"
  cre_cli_binary_path = "./cre_v0.1.5_linux_amd64"

  [workflow_config.compiled_config]
    binary_url = "https://gist.githubusercontent.com/Tofel/6b07d51e519cc48b829790567542992d/raw/943952cbff46333007fe4c6173da9db75390cf23/binary.wasm.br.b64"
    config_url = "https://gist.githubusercontent.com/Tofel/c334703c75bd457c6dcf260f1492616f/raw/cca378bb173fc79e524a7c7add12bb06dd39ba87/capabilities-don-config.json_17_03_2025"

[[nodesets]]
  nodes = 5
  override_mode = "each"
  http_port_range_start = 10100
  name = "workflow"

  [nodesets.db]
    image = "postgres:12.0"
    port = 13000
    volume_name = "workflow_volume"

  [[nodesets.node_specs]]

    [nodesets.node_specs.node]
      image = "injected-at-runtime"
      user_config_overrides = """
      [Feature]
			LogPoller = true

            [Log]
            Level = 'debug'
            JSONConsole = true

			[OCR2]
			Enabled = true
			DatabaseTimeout = '1s'

			[P2P.V2]
			Enabled = true
			ListenAddresses = ['0.0.0.0:5001']
      """

  [[nodesets.node_specs]]

    [nodesets.node_specs.node]
      image = "injected-at-runtime"
      user_config_overrides = """
      [Feature]
			LogPoller = true

            [Log]
            Level = 'debug'
            JSONConsole = true

			[OCR2]
			Enabled = true
			DatabaseTimeout = '1s'

			[P2P.V2]
			Enabled = true
			ListenAddresses = ['0.0.0.0:5001']
      """

  [[nodesets.node_specs]]

    [nodesets.node_specs.node]
      image = "injected-at-runtime"
      user_config_overrides = """
      [Feature]
			LogPoller = true

            [Log]
            Level = 'debug'
            JSONConsole = true

			[OCR2]
			Enabled = true
			DatabaseTimeout = '1s'

			[P2P.V2]
			Enabled = true
			ListenAddresses = ['0.0.0.0:5001']
      """

  [[nodesets.node_specs]]

    [nodesets.node_specs.node]
      image = "injected-at-runtime"
      user_config_overrides = """
      [Feature]
			LogPoller = true

            [Log]
            Level = 'debug'
            JSONConsole = true

			[OCR2]
			Enabled = true
			DatabaseTimeout = '1s'

			[P2P.V2]
			Enabled = true
			ListenAddresses = ['0.0.0.0:5001']
      """

  [[nodesets.node_specs]]

    [nodesets.node_specs.node]
      image = "injected-at-runtime"
      user_config_overrides = """
      [Feature]
			LogPoller = true

            [Log]
            Level = 'debug'
            JSONConsole = true

			[OCR2]
			Enabled = true
			DatabaseTimeout = '1s'

			[P2P.V2]
			Enabled = true
			ListenAddresses = ['0.0.0.0:5001']
      """

[[nodesets]]
  nodes = 3
  override_mode = "each"
  http_port_range_start = 10200
  name = "capabilities"

  [nodesets.db]
    image = "postgres:12.0"
    port = 13100
    volume_name = "capabilities_volume"

  [[nodesets.node_specs]]

    [nodesets.node_specs.node]
      image = "injected-at-runtime"
      user_config_overrides = """
      [Feature]
			LogPoller = true

            [Log]
            Level = 'debug'
            JSONConsole = true

			[OCR2]
			Enabled = true
			DatabaseTimeout = '1s'

			[P2P.V2]
			Enabled = true
			ListenAddresses = ['0.0.0.0:5001']
      """

  [[nodesets.node_specs]]

    [nodesets.node_specs.node]
      image = "injected-at-runtime"
      user_config_overrides = """
      [Feature]
			LogPoller = true

            [Log]
            Level = 'debug'
            JSONConsole = true

			[OCR2]
			Enabled = true
			DatabaseTimeout = '1s'

			[P2P.V2]
			Enabled = true
			ListenAddresses = ['0.0.0.0:5001']
      """

  [[nodesets.node_specs]]

    [nodesets.node_specs.node]
      image = "injected-at-runtime"
      user_config_overrides = """
      [Feature]
			LogPoller = true

            [Log]
            Level = 'debug'
            JSONConsole = true

			[OCR2]
			Enabled = true
			DatabaseTimeout = '1s'

			[P2P.V2]
			Enabled = true
			ListenAddresses = ['0.0.0.0:5001']
      """

[[nodesets]]
  nodes = 1
  override_mode = "each"
  http_port_range_start = 10300
  name = "gateway"

  [nodesets.db]
    image = "postgres:12.0"
    port = 13200

  [[nodesets.node_specs]]

    [nodesets.node_specs.node]
      image = "injected-at-runtime"
      user_config_overrides = """
      [Feature]
			LogPoller = true

            [Log]
            Level = 'debug'
            JSONConsole = true

			[OCR2]
			Enabled = true
			DatabaseTimeout = '1s'

			[P2P.V2]
			Enabled = true
			ListenAddresses = ['0.0.0.0:5001']
      """