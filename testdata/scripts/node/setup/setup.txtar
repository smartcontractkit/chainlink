exec chainlink node -s secrets.toml setup --chains evm,solana
cmp stdout out.txt

-- TODO CL_DATABASE_URL & flags

-- testdb.txt --
CL_DATABASE_URL
-- secrets.toml --
[Password]
Keystore = 'foobar'
VRF = 'baz42'

-- out.txt --
# *** partial config.toml generated by `chainlink node setup` ***

# TODO public keys
# TODO forwarder addresses
# TODO EVM.Workflow stuff?

[OCR2]
KeyBundleID = '0x1234'

[P2P]
PeerID = 'do we have to seed these?'
[P2P.V2]
DefaultBootStrappers = ['hmmmm']
