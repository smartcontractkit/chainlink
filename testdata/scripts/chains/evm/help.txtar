exec chainlink chains evm --help
cmp stdout out.txt

-- out.txt --
NAME:
   chainlink chains evm - Commands for handling evm chains

USAGE:
   chainlink chains evm command [command options] [arguments...]

COMMANDS:
   list  List all existing evm chains

OPTIONS:
   --help, -h  show help
   
