exec chainlink nodes starknet list --help
cmp stdout out.txt

-- out.txt --
NAME:
   chainlink nodes starknet list - List all existing StarkNet nodes

USAGE:
   chainlink nodes starknet list [arguments...]
