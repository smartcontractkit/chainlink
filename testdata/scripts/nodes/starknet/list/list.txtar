# start node
exec sh -c 'eval "echo \"$(cat config.toml.tmpl)\" > config.toml"'
exec chainlink node -c config.toml start -p password -a creds &

# initialize client
env NODEURL=http://localhost:$PORT
exec curl --retry 10 --retry-max-time 60 --retry-connrefused $NODEURL
exec chainlink --remote-node-url $NODEURL admin login -file creds --bypass-version-check

exec chainlink --remote-node-url $NODEURL nodes starknet list
cmp stdout out.txt

-- go:build.integration --
-- testdb.txt --
CL_DATABASE_URL
-- testport.txt --
PORT

-- password --
T.tLHkcmwePT/p,]sYuntjwHKAsrhm#4eRs4LuKHwvHejWYAC2JP4M8HimwgmbaZ
-- creds --
notreal@fakeemail.ch
fj293fbBnlQ!f9vNs

-- config.toml.tmpl --
[Webserver]
HTTPPort = $PORT
[Webserver.TLS]
HTTPSPort = 0

[[Starknet]]
ChainID = 'starknet chain ID'
FeederURL = 'http://feeder.url'

[[Starknet.Nodes]]
Name = 'first'
URL = 'https://starknet1.example'

[[Starknet.Nodes]]
Name = 'second'
URL = 'https://starknet2.example'

-- out.txt --

-----------------------------
Name:     first
Chain ID: starknet chain ID
State:    Unreachable
Config:   Name = 'first'
URL = 'https://starknet1.example'

-----------------------------
Name:     second
Chain ID: starknet chain ID
State:    Unreachable
Config:   Name = 'second'
URL = 'https://starknet2.example'

-----------------------------
