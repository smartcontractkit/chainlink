exec chainlink attempts list --help
cmp stdout out.txt

-- out.txt --
NAME:
   chainlink attempts list - List the Transaction Attempts in descending order

USAGE:
   chainlink attempts list [command options] [arguments...]

OPTIONS:
   --page value  page of results to display (default: 0)
   
