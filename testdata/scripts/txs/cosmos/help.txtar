exec chainlink txs cosmos --help
cmp stdout out.txt

-- out.txt --
NAME:
   chainlink txs cosmos - Commands for handling Cosmos transactions

USAGE:
   chainlink txs cosmos command [command options] [arguments...]

COMMANDS:
   create  Send <amount> of <token> from node Cosmos account <fromAddress> to destination <toAddress>.

OPTIONS:
   --help, -h  show help
   
