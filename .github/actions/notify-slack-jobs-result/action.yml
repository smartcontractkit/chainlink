name: Notify Slack Jobs Result
description: Posts a compact Slack notification for a workflow job result (status, job name, link to run). Set SLACK_BOT_TOKEN and SLACK_CHANNEL_ID in the job or workflow env for the step that invokes this action.
inputs:
  status:
    description: "Job conclusion, e.g. success, failure, cancelled, skipped (typically needs.<job>.result)"
    required: true
  job_name:
    description: "Display name for the job in the message"
    required: true
  run_url:
    description: "URL to the workflow run (e.g. format('https://github.com/{0}/actions/runs/{1}', github.repository, github.run_id))"
    required: true
  slack_thread_ts:
    description: "Optional Slack thread timestamp to reply in a thread"
    required: false
    default: ""

runs:
  using: composite
  steps:
    - name: Build Slack payload
      id: slack-payload
      shell: bash
      env:
        STATUS: ${{ inputs.status }}
        JOB_NAME: ${{ inputs.job_name }}
        RUN_URL: ${{ inputs.run_url }}
        THREAD_TS: ${{ inputs.slack_thread_ts }}
      run: |
        set -euo pipefail

        case "${STATUS}" in
          success)   EMOJI="✅" ;;
          failure)   EMOJI="❌" ;;
          cancelled) EMOJI="⚠️" ;;
          skipped)   EMOJI="⏭️" ;;
          *)         EMOJI="❔" ;;
        esac

        PAYLOAD_FILE="${RUNNER_TEMP:-/tmp}/notify-slack-jobs-result.json"

        jq -n \
          --arg channel "${SLACK_CHANNEL_ID}" \
          --arg job "$JOB_NAME" \
          --arg emoji "$EMOJI" \
          --arg status "$STATUS" \
          --arg url "$RUN_URL" \
          --arg thread "$THREAD_TS" \
          --arg text "${JOB_NAME} — ${STATUS}" \
          '{
            channel: $channel,
            text: $text,
            blocks: [
              {
                type: "header",
                text: {
                  type: "plain_text",
                  text: ($job + " " + $emoji),
                  emoji: true
                }
              },
              {
                type: "section",
                text: {
                  type: "mrkdwn",
                  text: ("Status: `" + $status + "`\n<" + $url + "|View workflow run>")
                }
              }
            ]
          }
          | if $thread != "" then .thread_ts = $thread else . end' \
          > "$PAYLOAD_FILE"

        echo "file=$PAYLOAD_FILE" >> "$GITHUB_OUTPUT"

    - name: Post to Slack
      uses: slackapi/slack-github-action@485a9d42d3a73031f12ec201c457e2162c45d02d # v2.0.0
      with:
        errors: "false"
        method: chat.postMessage
        token: ${{ env.SLACK_BOT_TOKEN }}
        payload-file-path: ${{ steps.slack-payload.outputs.file }}
