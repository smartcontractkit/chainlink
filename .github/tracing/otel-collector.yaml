receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "0.0.0.0:4317"
exporters:
  file:
    path: /tracing/trace-data.json
service:
  telemetry:
    logs:
      level: "debug"  # Set log level to debug
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [file]