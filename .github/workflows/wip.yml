name: test

on: [pull_request]

jobs:
  test:
    runs-on: [self-hosted]
    
    steps:
      - name: Setup
        run: |
          sudo ln -s /bin/true /bin/apt-get
      - name: Run
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: true
