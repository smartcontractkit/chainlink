name: test

on: [pull_request]

jobs:
  test:
    runs-on: [self-hosted]
    
    steps:
      - name: Test
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: true
