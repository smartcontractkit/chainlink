name: dependabump

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * 1-5" # every week-day at midnight

permissions: {}

jobs:
  dependabump:
    permissions:
      contents: write
      pull-requests: write
      actions: read
      security-events: read
      vulnerability-alerts: read
    uses: smartcontractkit/.github/.github/workflows/reusable-dependabump.yml@reusable-dependabump/0.1.0
    secrets:
      SLACK_TOKEN: ${{ secrets.DEPENDABUMP_SLACK_API_KEY }}
      SLACK_CHANNEL_ID: ${{ secrets.SLACK_TEAM_CORE_CHANNEL_ID }}
