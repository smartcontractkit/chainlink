#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
TOOLS_TEST_BIN="${ROOT}/tools/test/.bin/cltest"

mkdir -p "$(dirname "${TOOLS_TEST_BIN}")"

go -C "${ROOT}/tools/test" build -o "${TOOLS_TEST_BIN}" .

exec "${TOOLS_TEST_BIN}" "$@"
