#!/bin/bash

# Environment variables used for chainlink/ethereum tests

export ETH_PORT=${ETH_WS_PORT=18546}
export ETH_URL=${ETH_URL=ws://127.0.0.1:$ETH_WS_PORT}

export ETH_HTTP_PORT=${ETH_HTTP_PORT=18545}
export ETH_HTTP_URL=${ETH_HTTP_URL=http://127.0.0.1:$ETH_HTTP_PORT}

export ETH_CHAIN_ID=${ETH_CHAIN_ID=34055}
