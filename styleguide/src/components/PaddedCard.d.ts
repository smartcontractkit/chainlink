import React from 'react';
interface IProps {
    children: React.ReactNode;
    className?: string;
}
declare const PaddedCard: ({ children, className }: IProps) => JSX.Element;
export default PaddedCard;
