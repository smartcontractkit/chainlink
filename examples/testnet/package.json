{
  "name": "@chainlink/example-testnet",
  "private": true,
  "version": "0.6.0",
  "license": "MIT",
  "scripts": {
    "depcheck": "echo \"@chainlink/example-testnet\" && depcheck --ignore-dirs=contracts || true",
    "solhint": "solhint ./contracts/**/*.sol",
    "lint": "yarn solhint",
    "setup": "echo \"No setup required for @chainlink/example-testnet\""
  },
  "devDependencies": {
    "depcheck": "^0.8.3",
    "solc": "0.4.24",
    "solhint": "^2.1.0",
    "truffle": "^5.0.25"
  }
}
